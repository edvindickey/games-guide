require 'rails_helper'

RSpec.describe "articles/edit", type: :view do
# let!(:user) { create(:user) }

# it "should rendered partial:'articles/form'" do
#   allow(view).to receive(:current_user).and_return(user)
#   stub_template "articles/_form.html.erb" => "Create new article"
#   render
#   expect(rendered).to match "Create new article"
# end

# before do
#  @article = assign(:article, article.create!(title: "a", body: "b", user: user))
# end

# it "should show form for 'Create new article'" do
#   allow(view).to receive(:current_user).and_return(user)
#   render
#   expect(rendered).to include 'multipart/form-data'
# end

# it "should show data from article" do
#   allow(view).to receive(:current_user).and_return(user)
#   pp render
#   expect(rendered).to include "NewTester"
# end
end
